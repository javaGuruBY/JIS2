  1)объект-телефон.
      состояние:
              включен;
              выключен;
              в полете;
              не беспокоить;
              колличество заряда;
      поведение:
             позвонить(позволяет совершать звонки);
             играть в игры(открывает игру);
             проигрывать музыку(включает музыку);



2)объект-друг:
      состояния:
              веселый;
              грустный;
      поведение:
              помощь(помогает в трудных ситуациях);
              поддержка(поодерживает всегда и везде);

3)объект-преподователь:
      состояния:
              Злой;
      поведение:
             Унизить перед всем потоком(Вызвать к доске , и саддить перед всей группой);
             Лишить стипендии(отправить на пересдачу на экзамене);
             взять взятку(тут  объяснений не требуется :D);
             не принимать лабы(отказываться принять лабы вне урочное время);



 4)объект-электрочайник:
      состояния:
         объем,
         производитель
      поведение:
         греть воду;

  5)объект-машина:
      состояния:
         заведена;
         заглушена;
         в баке пусто;
      поведение:
        ехать;
        тормозить.
        
       

   6)объект-обувь:
       состояния:
          грязная;
          чистая;
          новая;
          поношенная;
       поведение:
          быть грязной в неподходящий момент;
          развязать шнурки;


     7)объект-холодильник:
      состояния:
          колличество еды ;
          объем;
      поведение:
          замараживать еду(замарозить еду лежащюю на верхней полке в холодильнике);

      8)объект-ноутбук:
           состояния:
              процент зарядки;
              разрешение экрана;
              фирма;
              год выпуска;
           поведение:
              гуглить что нибуть,
              делать домашку.

      9)объект-носки:
            состояния:
              порванные;
              целые;
              порванные , но еще носить можно;
            поведение:
              греть;
              вонять;

     
              
        
        
